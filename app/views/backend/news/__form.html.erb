<%= form_with(model: backend_news, local: true) do |form| %>
  <% if backend_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backend_news.errors.count, "error") %> prohibited this backend_news from being saved:</h2>

      <ul>
      <% backend_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
    
      
  </div>
<% end %>
